#====================== 自适应 RAG（进阶） =======================
# ===========================================================
# 自适应 RAG 入口模块（简化版）
# 功能：统一封装 RAG 流程，后续可扩展查询优化、路由检索、多策略切换
# ===========================================================

# 导入基础 RAG 问答链
from .rag_chain import get_rag_chain


def get_adaptive_rag():
    """
    🌟 自适应 RAG 入口（简化工业级版本）
    作用：
        1. 统一创建 RAG 问答链
        2. 未来可扩展：是否需要检索、是否需要改写问题、是否需要多轮召回
        3. 让主程序调用更简单
    
    返回：
        完整可调用的 RAG 对话链
    """
    # 直接返回构建好的 RAG 链（后续可在这里加动态判断逻辑）
    return get_rag_chain()