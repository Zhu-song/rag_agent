#=================== 重排序模型（BGE、m3e）============================================
# 一句话：把检索结果再精简，只留最有用的 TOP_N 条。
# 检索可能返回 10 条，重排只留最相关 3～5 条
# 减少大模型输入，提升回答质量
# 后续可以换成 bge-reranker 模型，效果更强
# ===========================================================
# 文档重排序模块（Rerank）
# 作用：对检索回来的多篇文档做二次排序，只保留最相关的 TOP_N 篇
# 简易版：直接截取前 N 篇，后续可替换成 bge-reranker 等模型增强效果
# ===========================================================

# 从配置文件导入：重排序后保留的 TOP_N 数量
from config import RERANK_TOP_N


def rerank_documents(query: str, docs: list[str]) -> list[str]:
    """
    文档重排序函数（简易基础版）
    作用：对检索返回的文档进行精简，只保留最靠前的高相关性文档
    特点：无模型、速度极快、适合快速搭建 RAG 流程
    
    :param query: 用户的问题（用于后续模型重排序，简易版暂未使用）
    :param docs: 检索返回的文档列表
    :return: 重排序 + 精简后的文档列表
    """
    # 简易策略：直接截取前 N 条最相关的文档（后续可替换成真实重排序模型）
    return docs[:RERANK_TOP_N]